# KOSIS MCP 의존성 — Python 3.11+

# MCP SDK (1.2.0+ required for StreamableHTTPSessionManager)
mcp>=1.2.0

# 웹 서버 (SSE transport)
uvicorn[standard]>=0.30.0
starlette>=0.37.0

# CORS 미들웨어
starlette[full]>=0.37.0

# 비동기 HTTP 클라이언트
httpx>=0.27.0

# 데이터 처리 (카테고리 필터링, 요약 통계)
pandas>=2.1.0

# 환경변수 (.env 파일 지원)
python-dotenv>=1.0.0
