[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn server:starlette_app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
